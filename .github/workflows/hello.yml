name: hello world

on:
    push:
       branches:
          -main

jobs: 
    Ejecutar_helloworld:
        runs-on: ubuntu-latest

        steps:
            - name: say hello
              run: echo "hello world"
